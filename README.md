# tabby-configurable-line-breaks
This Tabby plugin allows you to map keyboard shortcuts to line breaks in the terminal.  Handy for using tools like Claude code and even more handy if you hate the default shift-alt-enter shortcut.
